services:
  api:
    build:
      context: .
      dockerfile: DockerFile
    image: web-search-agent-fastapi:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # Fallback if not set in .env
      PORT: "8000"
    restart: unless-stopped