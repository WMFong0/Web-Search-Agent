version: "3.13.12"
services:
  api:
    build:
      context: .
      dockerfile: DockerFile
    image: mannings-sasa-fastapi:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # Fallback if not set in .env
      PORT: "8000"
    restart: unless-stopped